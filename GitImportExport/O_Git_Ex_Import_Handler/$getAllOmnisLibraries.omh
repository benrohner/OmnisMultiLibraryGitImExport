If isclear(iPath2Plugins)
	Quit method lList
End If

Calculate lListMaster as $cinst.$getRecursivFileList(iPath2Plugins)
Do lListMaster.$search(pos('.lbs',$ref.name)>1,kTrue,kFalse,kTrue,kTrue)
Do lListMaster.$remove(kListKeepSelected)

Quit method lListMaster
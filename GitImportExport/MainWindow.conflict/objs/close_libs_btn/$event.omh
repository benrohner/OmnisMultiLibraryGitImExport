On evClick     ;; Event Parameters - pRow( Itemreference )
	Do $cinst.$enableButtons(kFalse)
	Do iGitHandler.$closeAllLibraries()
	Do $cinst.$enableButtons(kTrue)
	Do $cinst.$enableImpExpButtons(kFalse)